# 导出规则

> **触发词：** 当用户说 `导出` 时，Claude 必须执行以下流程。

## 触发词

| 用户说 | 行为 |
|--------|------|
| `导出第X章` 或 `导出 chapter X` | 导出指定章节 |
| `导出全部` 或 `全部导出` | 导出所有已完成的章节 |
| `导出` （无数字） | 默认导出最近修改的章节 |

## 执行命令

```bash
python3 export.py <章节号>
```

## 导出说明

- 源文件：`chapters/chapter-XX.md`
- 输出：`exports/第XXX章-标题.txt`（给番茄小说上传） + `第XXX章-标题.pdf`（分享给人看）
- 每次导出前**自动删除旧文件**，再生成新的，保证内容实时同步
- 标题自动从 MD 文件的 `## 第X章 标题` 解析
