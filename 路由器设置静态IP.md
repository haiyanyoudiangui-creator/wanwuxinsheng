# 路由器设置静态 IP — 小白也能看懂的教程

## 为什么要设置静态 IP？

**动态 IP（路由器默认）：** 每次设备重连 WiFi，IP 地址可能变。就像你去餐厅每次坐不同的桌子。

**静态 IP：** 固定不变。就像你的专属工位，永远是那张桌子。

**什么时候需要：**
- 你想用手机遥控数播（QPlay 投屏），设备 IP 变的话 App 找不到它
- 你用 ADB 连设备调试，IP 变了就连不上了
- 你家里有 NAS/打印机，需要一个固定的地址访问

---

## 第一步：找到你的路由器后台

1. 打开浏览器（Chrome/Safari 都行）
2. 在地址栏输入以下**三个地址之一**（挨个试，哪个能打开就用哪个）：

```
192.168.1.1
192.168.0.1
192.168.10.1
```

3. 出现登录页面 → 输入路由器的**管理员账号和密码**

> **账号密码在哪找？**
> 翻到路由器底部，贴着一张标签，上面写着"默认 IP""默认用户名""默认密码"。
> 通常是 `admin` / `admin` 或者 `admin` / `123456`。
> 如果你改过密码但忘了，用牙签捅路由器后面的 Reset 小孔 10 秒，恢复出厂设置，然后用标签上的默认密码。

---

## 第二步：找到"静态 IP"设置入口

不同路由器叫法不一样，找到最接近的就行：

| 路由器品牌 | 菜单路径 |
|-----------|---------|
| **小米** | 常用设置 → 局域网设置 → DHCP 静态 IP 分配 |
| **TP-Link** | 路由设置 → DHCP 服务器 → 静态地址分配 |
| **华为** | 更多功能 → 网络设置 → 局域网 → 静态 IP |
| **华硕** | 内部网络 → DHCP 服务器 → 手动指定 IP |
| **水星/腾达** | 高级设置 → DHCP 静态分配 |
| **其他品牌** | 搜"静态 IP" 或 "DHCP 保留" 或 "IP 绑定" |

> 👆 名字不一样但功能相同。只要菜单里有 **"绑定""静态""保留""固定 IP"** 这几个字，就是找对了。

---

## 第三步：添加一个静态 IP

找到入口后，你会看到一个表格，填写以下信息：

### 需要填的内容

| 字段 | 填什么 | 怎么找 |
|------|--------|--------|
| **设备名称** | 随意填，比如 "我的数播" | 你自己取名，方便记忆 |
| **MAC 地址** | 一串字母数字，如 `DC:C0:DB:D0:87:80` | 在设备的设置→关于→状态信息里找 |
| **IP 地址** | 如 `192.168.10.100` | 前三段和路由器一样，最后一段随便选个 100-200 之间的数字 |

### 怎么找 MAC 地址

**安卓设备：** 设置 → 关于手机/关于设备 → 状态信息 → WLAN MAC 地址

**iPhone：** 设置 → 通用 → 关于本机 → Wi-Fi 地址

**Windows：** 打开 CMD，输入 `ipconfig /all`，找 "物理地址"

**Mac：** 系统设置 → Wi-Fi → 详细信息 → MAC 地址

### IP 地址怎么填

假设你的路由器地址是 `192.168.10.1`：
- 前三段保持不变：`192.168.10.xxx`
- 最后一段填 **100 到 200 之间** 的数字（避免和其他设备冲突）
- 比如填 `192.168.10.100`

> ⚠️ **不能填 1**（那是路由器自己用的），**也不能填 255**（那是广播地址）

---

## 第四步：保存并重启

1. 点 **保存** 或 **应用**
2. 把被绑定的设备 **断开 WiFi 再重新连接**（关一下 WiFi 再打开）
3. 去设备的网络详情里看 IP，应该已经是刚才填的那个了

---

## 举个完整例子

> 我的数播设备 MAC 地址是 `DC:C0:DB:D0:87:80`
> 我的路由器地址是 `192.168.10.1`

**我怎么填：**
- 设备名称：`DMP-K7 数播`
- MAC 地址：`DC:C0:DB:D0:87:80`
- IP 地址：`192.168.10.100`

保存后，我手机上的 QPlay App 就永远用 `192.168.10.100` 去连这台数播，不用每次重新发现了。

---

## 常见问题

### Q: 保存的时候提示 "IP 地址已被占用"
选一个别的数字。比如把 100 改成 101。

### Q: 设置完连不上网了
检查填的 IP 前三段是否和路由器一样。路由器是 192.168.10.1，IP 就必须是 192.168.10.xxx。

### Q: 找不到 DHCP 静态分配的菜单
有些老旧路由器不支持这个功能。可以换个支持的路由器，或者在设备端设置静态 IP（难度更高一些）。

### Q: 小米路由器怎么找不到？
小米的入口比较隐蔽：**常用设置 → 局域网设置 → 拉到最下面 → 添加**

---

*让路由器记住你设备的"座位"，每次重连都不会迷路。*
